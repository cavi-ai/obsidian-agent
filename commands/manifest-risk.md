---
description: Build a grounded, ranked risk register from project notes and route selected mitigations into planning.
argument-hint: "[optional project/area focus]"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:manifest-risk`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
