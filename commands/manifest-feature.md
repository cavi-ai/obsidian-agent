---
description: Produce a prioritized, evidence-backed feature backlog from idea and feedback notes.
argument-hint: "[optional product/area focus]"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:manifest-feature`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
