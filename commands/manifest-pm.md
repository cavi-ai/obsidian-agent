---
description: Produce a prioritized, client-facing product roadmap from project notes and route selected work into the build workflow.
argument-hint: "[optional project/topic focus]"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:manifest-pm`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
