---
description: Produce a prioritized, evidence-backed content plan from vault knowledge and route the top pick into a grounded draft.
argument-hint: "[optional topic/audience focus]"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:manifest-content`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
