---
description: Map what the vault covers, surface specific knowledge gaps, and produce a prioritized research agenda.
argument-hint: "[optional topic/area focus]"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:manifest-research`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
