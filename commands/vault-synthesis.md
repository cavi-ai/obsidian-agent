---
description: Synthesize what the vault says about a topic with citations, contradictions, and gaps.
argument-hint: "<topic>"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:vault-synthesis`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
