---
description: Propose grounded infrastructure and system designs from architecture notes as a diagrammed Markdown brief.
argument-hint: "[optional system/area focus]"
---

# Claude compatibility adapter

Invoke **`obsidian-agent:manifest-infra`** with the user's arguments:

`$ARGUMENTS`

Follow that canonical skill exactly. This command contains no independent workflow logic.
