# OpenCode adapter

OpenCode loads the canonical portable workflows installed under its plugin
root. Preview the installation with `node scripts/install.mjs --host opencode
--scope user`; apply only by repeating the command with `--confirm <hash>`.

The adapter contains no workflow copies and requires no MCP server.
