# Version and support

This documentation describes Obsidian Agent 0.1.0, the first intentional public release of the existing portable workflow registry.

The immutable release manifest binds the documentation to tag `v0.1.0` and its full source commit. Repository tags and GitHub Release artifacts remain the authoritative release record.
